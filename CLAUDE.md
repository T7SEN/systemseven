# SystemSeven — Agent Entry Point

**`AGENTS.md` is the canonical agent guide for this repository** — read it for orientation, architecture, and the reference routing table. **`SKILL.md` is the pre-flight checklist and anti-hallucination inventory** — run its Section 0 before any code or proposal. **`references/`** holds on-demand deep dives; load them via the routing table in `AGENTS.md`, never preemptively. When in doubt, follow `AGENTS.md`.
